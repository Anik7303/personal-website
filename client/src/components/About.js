import React from "react";

import "./About.scss";

const About = (props) => {
    return (
        <div>
            <h2 className="heading-2">About</h2>
        </div>
    );
};

export default About;
